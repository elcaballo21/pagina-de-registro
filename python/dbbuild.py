# import sqlite3


# with sqlite3.connect("database.db") as conn:
#     cursor = conn.cursor()
#     try:
#         cursor.execute("CREATE TABLE usuario(user TEXT, password TEXT, PRIMARY KEY(user))")
#     except Exception as e:
#         print("Hubo un error al intentar crear una tabla")
#     else:
#         print("Tabla creada correctamente")


        